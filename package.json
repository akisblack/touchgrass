{
  "name": "touchgrass",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev:web": "vite",
    "preview:web": "vite preview",
    "build:web": "vite build",
    "ts:web": "svelte-check --tsconfig ./tsconfig.json",

    "preview:electron": "vite build && npx cap sync && npx cap open @capacitor-community/electron",
    "build:electron": "vite build && npx cap sync && cd electron && npm run electron:make-all",
    "build:electron-win": "vite build && npx cap sync && cd electron && npm run electron:make-win",
    "build:electron-linux": "vite build && npx cap sync && cd electron && npm run electron:make-linux"
  },
  "devDependencies": {
    "@capacitor/cli": "^3.4.3",
    "@sveltejs/vite-plugin-svelte": "^1.0.0-next.30",
    "@tsconfig/svelte": "^2.0.1",
    "svelte": "^3.44.0",
    "svelte-check": "^2.2.7",
    "svelte-preprocess": "^4.9.8",
    "tslib": "^2.3.1",
    "typescript": "^4.5.4",
    "vite": "^2.9.5"
  },
  "dependencies": {
    "@capacitor-community/electron": "^4.1.0",
    "@capacitor/android": "^3.4.3",
    "@capacitor/core": "^3.4.3"
  }
}